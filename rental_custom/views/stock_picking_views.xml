<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_create_sale_order" type="object" string="Facturar las Faltas"
                        class="btn-primary"
                        groups="rental_custom.group_faltas_alquiler"
                        invisible="not is_rental_order or not backorder_id or sale_order_id or state in ('done', 'cancel')"/>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="after">
                <field name="qty_missing"
                       string="Faltas"
                       column_invisible="parent.state in ('done', 'cancel') or not parent.is_rental_order or parent.picking_type_code != 'incoming'"/>
            </xpath>
        </field>
    </record>
</odoo>

